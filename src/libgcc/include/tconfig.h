/* rac1-decomp: stand-in for the tconfig.h a GCC build tree generates.
   libgcc2.c needs only these facts about the EE target to build the
   modules used here. Not part of GCC's source. */
#define BITS_PER_UNIT 8
#define WORDS_BIG_ENDIAN 0
#define LONG_DOUBLE_TYPE_SIZE 64
#define inhibit_libc
#define SUPPORTS_WEAK 0
