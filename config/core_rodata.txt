# Read-only data that compiled core objects provide themselves, in place of
# the retail blob's copy in core_rdata: the object, then the labels its
# .rodata replaces (consecutive, in address order). tools/build_sn_data.sh
# cuts core_rdata around them and rac1.ld.sh puts the object's .rodata in
# the hole (tools/core_rodata.py). The object's .rodata must be exactly
# those bytes, and nothing else.
build-sn/core/00119D88.o D_00152898 D_001528A0 D_001528A8
build-sn/libgcc/l2_divdi3.o D_00152B18
